# DEPRECATED

This directory (`backend/services/`) is part of the **legacy system** and has been superseded by `backend/app/services/`.

## Migration Status

| Old Module | New Module | Status |
|---|---|---|
| All legacy services | `backend/app/services/` | ✅ Migrated |

## Notes

- Do **NOT** add new code to this directory.
- All business logic now uses `backend/app/services/`.
- This directory is kept for backward compatibility only and will be removed in a future release.

---

Created by Person D during legacy system cleanup (team-division.md §人员 D §7).
