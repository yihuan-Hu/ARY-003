# Copy values into your shell or runtime environment.
# The application does not automatically load this file.

FLASK_CONFIG=development
ORGANIZER_DB=organizer.db

# Replace both values outside local development.
ARY_JWT_SECRET=replace-with-a-long-random-secret
ARY_SUBMISSION_SECRET=replace-with-a-long-random-secret
